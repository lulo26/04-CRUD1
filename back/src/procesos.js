ejemplo = process.env; // api node process :: todas las variables de configuración
console.log(ejemplo);

// variables de configuración detalladas
console.log(
  process.env.USERNAME,
  process.env.OS,
  process.env.NUMBER_OF_PROCESSORS
);
